/**
 * Elements with attributes `data-animate-in`, `data-animate-highglight`, `data-animate-out` will animate.
 * Animations have `data-animate-*-duration` properties for duration, and `data-animate*-delay` for delay (those can be inherited from parent elements).
 *
 * Use CSS' new typed attribute function to handle duration and delay.
 *
 * Add animation stylesheet and dynamically load it based on presence of data attributes.
 *
 * Add transitions stylesheet based on slide transitions. Use view transition API?
 */
